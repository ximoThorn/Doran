import DrCheckboxGroup from '../checkbox/src/checkbox-group.vue';

DrCheckboxGroup.install = function (Vue) {
  Vue.component(DrCheckboxGroup.name, DrCheckboxGroup);
};

export default DrCheckboxGroup;
