import DrBreadcrumbItem from '../breadcrumb/src/breadcrumb-item.vue';

DrBreadcrumbItem.install = function (Vue) {
  Vue.component(DrBreadcrumbItem.name, DrBreadcrumbItem);
};

export default DrBreadcrumbItem;
